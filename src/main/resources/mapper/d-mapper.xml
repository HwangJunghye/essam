<?xml version="1.0" encoding="UTF-8"?>
<!-- mapper(sql문) 작성 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.essam.www.d.IDDao">

	<select id="getMemberInfo" resultType="MemberBean">
		SELECT * FROM MEMBER WHERE MBID=#{mbId}
	</select>
	
	<update id="memberUpdate">
		UPDATE MEMBER SET mbPwd, mbNickname, mbTel, mbAddr, mbBirth, mbGender, cate1No, cate2No
		 = mbPwd, mbNickname, mbTel, mbAddr, mbBirth, mbGender, cate1No, cate2No WHERE IS NOT NULL
	</update>

</mapper>