<?xml version="1.0" encoding="UTF-8"?>
<!-- mapper(sql문) 작성 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.essam.www.c.ICDao">
	<select id="getTeacherProfile" resultType="teacherbean">
		SELECT mbId, mbNickName, fileNo, teacherGrade, teacherIntro, teacherDetail 
		FROM TEACHER INNER JOIN MEMBER 
		ON TEACHER.MBID=MEMBER.MBID 
		WHERE MBID=#{mbId}
	</select>
</mapper>